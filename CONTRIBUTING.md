# Contributing

Fork the project, create a focused branch, add tests for behavior changes, run `npm run check && npm test`, and open a pull request describing the problem and solution. Keep dependencies minimal and never commit secrets or captured production payloads.
